# This will throw an error because the parser thinks the nested
#  elif is part of the first if. This is correct behavior.

func main
	int x = 10
	
	if (x == 10)
		println("Equal!")
		
		elif (x == 5)
			println("Equal to 5")
		end
	else
		println("Not equal!")
	end
end
